// Kit — time-of-day + weather enums (the only parts of the yokai game's random-event types the
// environment/weather systems need). The full random-event system was yokai-game-specific.
export type TimeOfDay = 'dawn' | 'day' | 'evening' | 'night';
export type WeatherCondition = 'clear' | 'rain' | 'fog';
