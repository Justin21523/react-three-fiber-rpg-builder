# Drop models here (alternative to src/assets/models/)

Put `.glb` / `.gltf` files here and they are auto-detected into Edit Mode's 🧊 Assets palette
(subfolders become categories). Files in `public/` are served as-is at `/models/...` — best for
large or many models. For bundled/hashed assets instead, use `src/assets/models/`.
